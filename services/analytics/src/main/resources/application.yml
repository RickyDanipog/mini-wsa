server:
  port: 8084
spring:
  application:
    name: analytics
wsa:
  storage: inmemory
  rate-limit:
    enabled: true
    max-requests: 100
    window-seconds: 60
management:
  endpoints:
    web:
      exposure:
        include: health,info
