# --- Java / Maven ---
target/
*.class
*.jar
*.war
!.mvn/wrapper/maven-wrapper.jar
dependency-reduced-pom.xml

# --- IntelliJ IDEA ---
.idea/
*.iml
*.ipr
*.iws
out/

# --- OS ---
.DS_Store

# --- Obsidian editor config ---
.obsidian/

# --- Env / secrets ---
*.env
.env.local

# --- Logs ---
*.log

# --- Local tooling config: ignore root .claude/ EXCEPT the shared skills ---
# (per-service .claude/ is tracked; skills are shipped so a cloner can use them)
/.claude/*
!/.claude/skills/

# --- Personal, local-only notes (never pushed) ---
*.local.md

# --- Completed-task review trail (local only) ---
/archives/

# --- Source assignment PDF: kept locally, never published (repo will go public) ---
Assignment(1).pdf
